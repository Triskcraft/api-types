# api-types

## Publishing

The package is published to npm through GitHub Actions trusted publishing.

Configure `@triskcraft/api-types` in npm with this trusted publisher:

- Repository: `Triskcraft/api-types`
- Workflow: `npm-publish.yml`

Then push a version tag such as `v1.2.1`, or run the `Publish to npm` workflow manually.
